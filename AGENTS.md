# AGENTS.md

Before doing any work in this repository, read **[CLAUDE.md](./CLAUDE.md)** in full — it is the authoritative source for the project's architecture, backend app layout, API endpoints, environment variables, workspace role hierarchy, persistent-storage lifecycle, session lifecycle, and local dev setup.

Everything an agent needs to orient itself (project structure, secrets policy, management commands, deployment notes) lives there.
